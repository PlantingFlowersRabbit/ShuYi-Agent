# v0.3.0 Verification

## Automated Verification

Run:

```bash
uv run pytest -q
cd frontend && npm run build
```

Coverage expectations:

- `tests/test_v0_30_batch_workflow.py` covers batch role selection, auto role/voice creation, grouped dubbing, export, and role deletion.
- Existing workflow tests confirm the API still starts the LangGraph AI one-click workflow, uses DeepSeek chapter-agent config for role analysis, and streams role-selection events after confirmation.
- Frontend repository checks confirm v0.3.0 title/version strings and the visible one-click analysis/dubbing/export controls.

## Manual / Real Environment Verification

- Start the backend and frontend, select a chapter, click `AI一键分析`, and confirm the role list is auto-updated before clicking `角色列表确认完成`.
- Click `一键生成配音`; verify progress/status shows grouped work, skipped count, and any failure reason.
- Click `一键导出`; verify per-statement files and `manifest.json`, and confirm `chapter_full.wav` appears only when all required statements have generated audio.
- If using the real Qwen3-TTS service, confirm `/v1/audio/speech-batch` returns one audio result per text for a shared reference voice.
