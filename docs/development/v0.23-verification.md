# v0.23 Verification

## Required Commands

```bash
python3 scripts/validate_harness.py
uv run --group dev ruff check .
uv run --group dev pytest -q
cd frontend && npm run build
```

## Startup Evidence

- Click `模型配置 -> 启动服务` with valid Base and VoiceDesign paths.
- Verify `启动服务进度` is visible while the backend waits for `/health`.
- Verify success text includes `模型加载完成`.
- Verify `/health` returns `ok=true`, `voice_clone=true`, `voice_design=true`, and `voice_design_capable=true`.

## VoiceGeneration Evidence

- After startup success, run `生成资源到资源库列表 -> 生成音色` and confirm it calls the VoiceDesign endpoint instead
  of returning a connection-refused placeholder.
- After startup success, run main-page `音频生成` for a role with reference audio and confirm it does not report
  `本地 TTS 未启动` unless the service actually becomes unavailable.
- Use a default MP3 reference voice resource during main-page `音频生成` and confirm the local JSON voice-clone path
  preserves the `.mp3` suffix before calling Qwen3-TTS.
