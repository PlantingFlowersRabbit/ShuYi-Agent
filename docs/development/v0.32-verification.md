# v0.3.2 Verification

## Automated Verification

Run:

```bash
uv run pytest -q
uv run ruff check .
cd frontend && npm run build
python3 scripts/validate_harness.py
```

Coverage expectations:

- `tests/test_v0_30_batch_workflow.py` covers the `“都怪你多嘴，她认出我们了。”佩罗恼火道。` regression by asserting AI语句划分 runs before the first batch role-selection call, the model only receives segmented utterances, and the speech tag is narrator-owned.
- `tests/test_repository_acceptance.py` confirms current version strings are `0.3.2`, docs index links v0.32 materials, and paragraph deletion no longer clears all AI role matching results.
- Existing workflow tests continue to cover `AI角色分析`, `AI角色匹配`, streamed role-selection events, batch dubbing, export, and role deletion constraints.

## Manual / Real Environment Verification

- Start backend, frontend, and local TTS services.
- Select a chapter containing `“都怪你多嘴，她认出我们了。”佩罗恼火道。`.
- Run `AI角色分析`, then `AI角色匹配`; confirm the dialogue fragment is assigned to 佩罗 and `佩罗恼火道。` is assigned to 旁白.
- After role matching completes, delete one unrelated paragraph from the right-hand chapter UI and confirm other paragraphs' matched utterances remain visible.
- Confirm `一键生成配音` and `一键导出` still operate on the remaining visible/matched utterances.
