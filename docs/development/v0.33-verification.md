# v0.3.3 Verification

## Automated Verification

Run:

```bash
uv run pytest -q
uv run ruff check .
cd frontend && npm run build
python3 scripts/validate_harness.py
```

Coverage expectations:

- `tests/test_v0_30_batch_workflow.py` covers `split_and_select` by asserting `AI角色匹配` receives the original mixed statement, splits it, assigns roles, and does not call the standalone AI statement segmentation service.
- `tests/test_v0_30_batch_workflow.py` covers the `“都怪你多嘴，她认出我们了。”佩罗恼火道。` regression by forcing an over-eager character role on the speech tag and verifying the backend stores it as narrator.
- `tests/test_repository_acceptance.py` confirms current version strings are `0.3.3`, docs index links v0.33 materials, and the frontend no longer contains the standalone `AI语句划分` button/function/progress label/API call.
- Existing workflow tests continue to cover `AI角色分析`, `AI角色匹配`, streamed role-selection events, batch dubbing, export, role deletion constraints, and paragraph deletion preservation.

## Manual / Real Environment Verification

- Start backend, frontend, and local TTS services.
- Select a chapter and click `确认无误`; confirm each visible paragraph has one editable whole-paragraph utterance draft.
- Confirm the paragraph toolbar only shows paragraph id, fold/unfold, and delete controls; no `AI语句划分` button is visible.
- Run `AI角色分析`, then `AI角色匹配` on a chapter containing `“都怪你多嘴，她认出我们了。”佩罗恼火道。`.
- Confirm `AI角色匹配` splits the mixed statement, assigns the quoted dialogue to 佩罗, and assigns `佩罗恼火道。` to 旁白.
- Delete one unrelated paragraph from the right-hand chapter UI and confirm other paragraphs' matched utterances remain visible.
