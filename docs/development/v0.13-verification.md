# v0.13 Verification

## Required Commands

```bash
python3 scripts/validate_harness.py
uv run --group dev pytest -q
cd frontend && npm run build
```

## UI Evidence

Use the real sample:

```text
/Users/gaojing/Downloads/真实测试样本/小说/这个地下城长蘑菇了.txt
```

Expected observations:

- Uploading the file updates `上传小说进度` and shows only the opening preview.
- Clicking `划分章节` creates the chapter list without loading a chapter body on the right.
- Selecting `1.变成蘑菇的公爵千金`, confirming paragraphs, then clicking `语句划分` does not show `paragraph not found`.
- Utterances are nested under their source paragraph and each editor field has a visible Chinese label.
- The utterance editor does not expose a `voice_cloning` selector.
- The button text is `音频生成`, not `音频试听`.
- The voice resource library page scrolls independently.
- The role play control is an icon button and attempts to play the selected voice resource.
- `新增角色` adds an editable role card.
- The model configuration page only shows remote Base URL, model name, api_key, local BASE_URL, and model weight path.

## Audio Evidence

- With local Qwen3-TTS running, capture `/health`, request payload summary, generated wav path, file size, and `ffprobe` duration.
- If Qwen3-TTS is not running, v0.13 may return a clearly marked substitute wav. Record the returned VoiceJob status as `substitute`, the warning text, and `ffprobe` duration.
- `mock 只能用于单元测试`; substitute wav proves graceful UI/API behavior, not real voice quality.

## Real Voice Samples

Check these local directories when present:

- `/Users/gaojing/Downloads/真实测试样本/音频/年轻男`
- `/Users/gaojing/Downloads/真实测试样本/音频/御姐音`
- `/Users/gaojing/Downloads/真实测试样本/音频/播音腔女`
- `/Users/gaojing/Downloads/真实测试样本/音频/男声旁白`

Each mp3 must be playable or decodable by `ffprobe`, and each `语音内容.txt` must be readable.
