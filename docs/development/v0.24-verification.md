# v0.24 Verification

## Required Commands

```bash
python3 scripts/validate_harness.py
uv run --group dev ruff check .
uv run --group dev pytest -q
cd frontend && npm run build
```

## AI Segmentation Evidence

- Select a chapter and click `确认无误`.
- Confirm every visible paragraph now contains one editable `语句文本` equal to the full paragraph.
- Confirm the chapter header no longer shows the old global `语句划分` button.
- Confirm each paragraph toolbar shows `AI语句划分` before `折叠`.
- Click `AI语句划分` on one paragraph and confirm only that paragraph's utterance drafts are replaced.
- For a paragraph containing dialogue + narration + dialogue, confirm the result follows speaker/role units rather than
  punctuation-only sentence splitting.

## Agent Evidence

- Confirm the segmentation provider defaults remain SiliconFlow `https://api.siliconflow.cn/v1` and `Qwen/Qwen3-8B`.
- Confirm backend tests cover the LangChain/reflection path: first output fails text conservation, reflection output
  passes and preserves the exact source text.

## Voice Binding Evidence

- Add or save a voice resource named `男2` with description `雄壮苍老的声音`, reference text
  `这是一段用于试听新音色的语音。`, and reference audio `outputs/audio/preview-0002.wav`.
- Create or update role `旁白2` to select `男2`.
- Generate audio from a `语句文本` using role `旁白2`.
- Confirm the backend voice job records `reference_audio_path=outputs/audio/preview-0002.wav` or the absolute path to
  that file, and `reference_text=这是一段用于试听新音色的语音。`.
