# v0.1 验证记录

本文记录 v0.1 人工协作版实现后的验证命令、真实环境证据要求和当前未覆盖项。mock 只能用于单元测试，不得替代端到端结论。

## 自动验证

```bash
python3 scripts/validate_harness.py
UV_CACHE_DIR=.uv-cache uv run --group dev pytest -q
UV_CACHE_DIR=.uv-cache uv run --group dev --group backend pytest tests/test_domain_workflow.py -q
cd frontend && npm install && npm run build
```

## 真实环境证据

- SiliconFlow：真实语句划分需要记录 provider、base_url、model、输入段落、原始输出、JSON repair 结果、schema 校验和文本守恒结果。
- Qwen3-TTS：真实音频试听需要记录服务启动命令、`QWEN3_TTS_MODEL_PATH`、device、request text、reference audio、reference text、输出路径、文件大小和 ffprobe 时长。
- UI 截图：真实浏览器验收需要保存小说导入、章节列表、段落模块、确认门禁、语句划分结果、角色同步和音频试听截图。
- 音频样本：manifest、许可证、transcript、本地文件、ffprobe 可解码结果和时长都需要保留证据。

## 当前证据状态

- 已完成：harness 文件、manifest、样本音频 decode、域逻辑单元测试、API 路由边界测试、前端 API 调用结构检查、React/Vite 生产构建、SiliconFlow 在线 LLM smoke、本地 Qwen3-TTS 服务健康检查和真实合成 smoke、浏览器 UI 截图验收。
- 2026-07-29 自动验证结果：
  - `python3 scripts/validate_harness.py` 通过。
  - `UV_CACHE_DIR=.uv-cache uv run --group dev pytest -q` 通过，17 passed，1 个 Starlette/httpx deprecation warning。
  - `UV_CACHE_DIR=.uv-cache uv run --group dev --group backend pytest tests/test_domain_workflow.py -q` 通过，13 passed，1 个 Starlette/httpx deprecation warning。
  - `UV_CACHE_DIR=.uv-cache uv run --group dev ruff check backend/app tests` 通过。
  - `cd frontend && npm run build` 通过。
- 已补强：前端通过 `/api/novels/parse`、`/api/roles/{role_id}`、`/api/paragraphs/{paragraph_id}/segment` 和 `/api/utterances/{utterance_id}/speech` 连接后端工作流；Vite dev server 通过 `frontend/vite.config.ts` 代理 `/api` 和 `/outputs` 到 FastAPI；后端语句划分默认调用 SiliconFlow provider，JSON repair 在 API 路径最多执行一次并保留原始模型输出；TTS 试听端点调用本地 Qwen3-TTS 服务、验证 wav 可解码且时长大于 0.5 秒，并返回 `/outputs/audio/*.wav` 试听 URL。
- SiliconFlow 真实环境证据：
  - `.env` 中 `SILICONFLOW_API_KEY` 已存在；验证日志只记录 present/length，不输出密钥原文。
  - Provider：`siliconflow-qwen3-8b`，base_url `https://api.siliconflow.cn/v1`，model `Qwen/Qwen3-8B`，`max_tokens` 768，`enable_thinking` false。
  - 失败边界样本 1：旧裸 HTTP payload 把 `enable_thinking` 放入 `extra_body` 且无 `max_tokens` 时，真实请求 60 秒 read timeout；已改为把 `extra_body` 展开为真实请求体字段，并设置 `max_tokens`。
  - 失败边界样本 2：输入段落 `宝玉笑道：“这个妹妹我曾见过的。”黛玉只是不语。` 返回完整 schema，但模型把中文双引号改成中文单引号；文本守恒返回 `text_conservation_failed`，后端正确拦截。
  - 成功样本：`paragraph_id` `real-sf-p-0004`，输入段落 `宝玉笑道，妹妹我曾见过的。黛玉只是不语。`，原始输出为两条 utterance：`宝玉笑道，妹妹我曾见过的。` / `黛玉只是不语。`，schema 校验通过，`repaired` false，文本守恒通过。
- Qwen3-TTS 真实环境证据：
  - 模型路径：`/Users/gaojing/Documents/002-研究生/993-其他/微信小程序/app_mac/models/Qwen3-TTS-12Hz-1.7B-Base`。
  - Python 运行时：`/Users/gaojing/Documents/002-研究生/993-其他/微信小程序/app_mac/.venv-qwen3-tts/bin/python`，`import qwen_tts, soundfile, torch, fastapi` 通过。
  - 服务启动命令：`/Users/gaojing/Documents/002-研究生/993-其他/微信小程序/app_mac/.venv-qwen3-tts/bin/python backend/tts/qwen3_tts_server.py --model-path /Users/gaojing/Documents/002-研究生/993-其他/微信小程序/app_mac/models/Qwen3-TTS-12Hz-1.7B-Base --host 127.0.0.1 --port 7811 --device cpu`。
  - `/health` 返回 `{"ok":true}`。
  - 失败边界样本：`outputs/audio/real-qwen3-tts-smoke.wav`，ffprobe duration `0.160000`、size `7724`，`validate_wav_duration` 拒绝：`generated audio duration must be > 0.5s, got 0.160s`。
  - 成功合成样本：`outputs/audio/real-qwen3-tts-shortref-shorttext-icl.wav`，request text `他说：你好，欢迎来到这个新的故事。`，reference audio `assets/samples/voices/cmn_qixinxieli_canonni_cc0.wav`，reference text `齐心协力`，ffprobe duration `3.760000`、size `180524`，`validate_wav_duration` 通过。
  - 交叉检查样本：`outputs/audio/real-qwen3-tts-shortref-shorttext-xvec.wav` duration `3.440000`、`real-qwen3-tts-shortref-shorttext-icl_nonstream.wav` duration `3.440000`、`real-qwen3-tts-shortref-shorttext-icl_more_tokens.wav` duration `3.600000`，均通过后端 WAV 时长校验。
  - UI 成功合成样本：`outputs/audio/vj-0001.wav`，由浏览器点击 `TTS 试听` 触发 `/api/utterances/p-0001-u-001/speech`，ffprobe duration `2.880000`、size `138284`，`validate_wav_duration` 通过。
- UI 截图证据：
  - 服务：Qwen3-TTS `127.0.0.1:7811`，FastAPI `127.0.0.1:8000`，Vite `127.0.0.1:5173`。
  - Chrome headless/CDP 验收页面 `http://127.0.0.1:5173/`，真实经过 Vite proxy、FastAPI、SiliconFlow 和 Qwen3-TTS。
  - 截图路径：`outputs/ui-evidence/11-initial-workbench-after-real-env.png`、`12-imported-via-backend.png`、`13-confirmed-gate-open.png`、`14-segmented-utterances.png`、`15-tts-preview-success.png`。
  - 页面 checkpoint：导入成功 true，确认门禁 true，语句划分完成 true，TTS 成功 true，TTS 失败 false，`audioSrc` `/outputs/audio/vj-0001.wav`，`audioReadyState` 4。
- 风险说明：SiliconFlow 对含中文双引号文本仍可能改写标点，当前依赖文本守恒失败提示进入人工修正；`voice_design` 是 UI/请求合约预留，本地 Qwen3 合成路径仍以 voice cloning smoke 为主。
