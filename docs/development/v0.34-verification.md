# v0.3.4 Verification

## Automated Verification

Run:

```bash
uv run pytest -q
cd frontend && npm run build
python3 scripts/validate_harness.py
```

Coverage expectations:

- `tests/test_repository_acceptance.py` confirms version strings are `0.3.4`.
- `tests/test_repository_acceptance.py` confirms the frontend uses `AI小说格式解析` and no longer exposes old `AI章节划分` copy.
- `tests/test_repository_acceptance.py` confirms the collapsible chapter rail, `当前章节完整小说内容`, and `划分语句与角色匹配` workspace are present.
- Existing workflow tests continue to cover `AI角色分析`, `AI角色匹配`, streamed role-selection events, batch dubbing, export, role deletion constraints, and backend text-conservation behavior.

## Manual / Real Environment Verification

- Start backend and frontend services.
- Upload a TXT or EPUB novel.
- Click `AI小说格式解析`; confirm the left chapter list is populated.
- Collapse and expand the chapter rail; confirm the current chapter workspace changes width without text overlap.
- Select a chapter; confirm the left half shows the complete chapter body and the right half contains only statement/role/audio controls.
- Click `AI角色分析`; confirm the role list is populated or updated.
- Click `AI角色匹配`; confirm statements appear in the right panel with role selectors and no manual paragraph cards.
- Click `一键生成配音`; confirm generated audio statuses update per statement.
