# v0.11 Verification

## Required Commands

```bash
python3 scripts/validate_harness.py
uv run --group dev pytest -q
cd frontend && npm run build
```

## Real Sample Evidence

Use the local sample directory:

```text
/Users/gaojing/Downloads/真实测试样本
```

Novel import check:

- File: `小说/这个地下城长蘑菇了.txt`
- Expected: chapter parser recognizes numeric headings such as `1.变成蘑菇的公爵千金`.
- Expected: it does not fall back to `未分章正文`.

Voice library check:

- `年轻男/年轻男.mp3` with `年轻男/语音内容.txt`
- `御姐音/御姐音.mp3` with `御姐音/语音内容.txt`
- `播音腔女/播音腔女.mp3` with `播音腔女/语音内容.txt`
- `男声旁白/男声旁白.mp3` with `男声旁白/语音内容.txt`

Audio evidence must include `ffprobe` duration and decode success for each mp3.

## Provider Evidence

真实环境结论必须来自新鲜命令输出、截图或音频元数据，不能只来自代码阅读。

SiliconFlow segmentation still requires:

- provider name
- base URL
- model
- raw output
- repair output
- JSON schema result
- text conservation result

Qwen3-TTS still requires:

- `QWEN3_TTS_MODEL_PATH`
- service health result
- request text
- reference audio
- reference text
- output path
- file size
- `ffprobe` duration

`mock 只能用于单元测试`; deterministic generated voice substitutes cannot be used as proof that real Qwen3-TTS or voice
design quality works.

## UI Evidence

UI 截图 must include:

- v0.11 top tabs: 主页面, 音色资源库, 模型配置
- main page chapter list after importing the real novel sample
- role list with voice selector and playable reference audio controls
- paragraph cards with nested utterance editors
- voice resource library list, add form, generate form, save button, and checked delete state
- model configuration page without visible API key values

Screenshots should cover desktop and mobile widths and must show no text overlap or button overflow.

## Current Local Notes

- Real sample mp3 files are local test inputs and are not committed to the repository.
- Generated voice resource audio in v0.11 uses a local deterministic substitute unless a real provider run is explicitly
  recorded.
