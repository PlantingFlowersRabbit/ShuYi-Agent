# v0.3.1 Verification

## Automated Verification

Run:

```bash
uv run pytest -q
uv run ruff check .
cd frontend && npm run build
python3 scripts/validate_harness.py
```

Coverage expectations:

- `tests/test_repository_acceptance.py` confirms the current version strings are `0.3.1`.
- Frontend repository checks confirm the old `AI一键分析` entry is split into `AI角色分析` and `AI角色匹配`.
- Existing workflow tests continue to verify the first stage creates or updates roles and binds voices, while the resume stage performs streamed statement role selection.
- `tests/test_v0_30_batch_workflow.py` continues to cover batch role selection, `needs_split`, grouped dubbing, export, and role deletion behavior.

## Manual / Real Environment Verification

- Start backend, frontend, and local TTS services.
- Select a chapter, click `AI角色分析`, and confirm the role list is auto-updated with bound or generated voices.
- After reviewing roles, click `AI角色匹配` and confirm blank-role statements receive roles through streamed progress events.
- Confirm old `AI一键分析` and `角色列表确认完成` action labels are not visible in the main workflow.
- Click `一键生成配音` and `一键导出` only after role matching, verifying audio generation/export still remain explicit follow-up actions.
