# v0.25 Verification

## Required Commands

```bash
python3 scripts/validate_harness.py
uv run --group dev ruff check .
uv run --group dev pytest -q
cd frontend && npm run build
```

## AI One-Click Evidence

- Run AI chapter splitting, select a chapter, and confirm `AI一键分析` appears immediately left of `确认无误`.
- Click `AI一键分析` and confirm role candidates appear under `角色列表` with evidence, confidence, and human-review language.
- Add or bind the needed roles manually, then click `角色列表添加完成`.
- Confirm whole-paragraph utterance drafts are created from backend `utterance_drafts`, not local button-click simulation.
- Confirm blank-role utterances are assigned one by one as streamed updates; already assigned roles are not overwritten.
- Confirm ambiguous multi-speaker paragraphs use the existing backend `AI语句划分` service and preserve text conservation.
- Confirm failures from invalid JSON/schema/text conservation are readable and do not rewrite source text.
- Confirm no audio generation starts automatically.
