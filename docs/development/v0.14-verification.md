# v0.14 Verification

## Required Evidence

- Confirm a locally edited/deleted chapter paragraph and verify no `paragraph not found` message is shown.
- Run segmentation after confirmation with a configured Qwen/Qwen3-8B provider and record raw segmentation status, repair status, and text conservation result.
- Use the dialogue sample `“别笑了，引来魔物就麻烦了。”佩罗不耐烦地打断了笑声，“就这了，把她放下来。”` and verify it splits into dialogue / narration / dialogue speaker units.
- Verify a full same-speaker quote such as `“因为你爹就要完啦，你真以为他能挡住帝国的进攻么？而弄死你就是压倒他最后的稻草，弄死你老子也能混到个贵族当当。”` remains one unit unless the model flags uncertainty.
- Capture UI evidence that paragraph collapse hides nested audio-generation modules and that each module has add/delete controls.
- Capture UI evidence for Chinese emotion select, `其他控制文本`, language, x-vector-only, speed note, and volume note.
- Add a voice resource with the file picker, generate a voice preview with progress, play the preview, and save only after audition.
- Save remote and local model configs separately; test the remote link and start the local TTS service, recording success feedback.
- Confirm desktop-only layout has no mobile media query and keeps the workbench at a desktop minimum width.

## Model Capability Notes

- Local `Qwen3-TTS-12Hz-1.7B-Base` voice cloning is driven by reference audio/text, language, response format, and x-vector mode.
- Qwen3-TTS VoiceDesign / Instruct paths use natural-language control text; the UI keeps user-entered control text in Chinese.
- If a real voice-design model is not configured, generated voice preview may be a clearly marked local deterministic substitute and must not be represented as real voice-design quality.
