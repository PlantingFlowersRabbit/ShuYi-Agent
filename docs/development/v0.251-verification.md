# v0.251 Verification

## Required Commands

```bash
python3 scripts/validate_harness.py
uv run --group dev ruff check .
uv run --group dev pytest -q
cd frontend && npm run build
```

## DeepSeek Evidence

- In the model configuration page, set the AI章节划分智能体 Base URL to `https://api.deepseek.com/v1`, use a valid DeepSeek key, and confirm `测试链接` succeeds without `Not Found`.
- Start `AI一键分析` after selecting a chapter and confirm the role-analysis phase uses the AI章节划分智能体 DeepSeek configuration.
- Confirm the workflow still pauses after role candidates and shows `角色列表添加完成`.
- Resume the workflow and confirm paragraph AI segmentation still uses the existing SiliconFlow `Qwen/Qwen3-8B` service when segmentation is needed.
- Confirm no audio generation starts automatically and text-conservation failure behavior is unchanged.
